/**
 * Copy this file to `public/firebase-config.js` and fill in your real Firebase credentials.
 * Files inside `public/` are served as-is, so do NOT commit secrets. The real config file
 * is ignored via .gitignore and can safely live only in your local environment or build pipeline.
 */

window.__FIREBASE_CONFIG__ = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_AUTH_DOMAIN',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_STORAGE_BUCKET',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
